{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "env": {
    "POSTGRES_URL": "@postgres_url",
    "BLOB_READ_WRITE_TOKEN": "@blob_read_write_token",
    "JWT_SECRET": "@jwt_secret"
  }
}
